import React from 'react';
import { logoWeed } from '../pictures';

const Header = () => {
  return (
    <header className="header">
      <img src={logoWeed} />
    </header>
  );
};

export default Header;
